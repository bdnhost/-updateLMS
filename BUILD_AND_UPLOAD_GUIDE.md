# 🚀 מדריך Build והעלאה דרך FTP

## שלב 1: Build מקומי 💻

פתח CMD/Terminal ב-Windows והרץ:

```bash
cd C:\Users\User\Desktop\updateLMS\LMS

# התקן תלויות (רק בפעם הראשונה)
npm install

# בנה את האפליקציה
npm run build
```

### מה זה עושה?
- קורא את כל קבצי ה-React/JSX
- מאחד אותם לקובץ JavaScript אחד
- ממזער את הקוד (minification)
- יוצר תיקייה `dist/` עם הקבצים המוכנים

### זמן משוער:
- `npm install` - 2-5 דקות (רק בפעם הראשונה)
- `npm run build` - 30-60 שניות

---

## שלב 2: בדוק שה-Build הצליח ✅

אחרי `npm run build`, בדוק:

```bash
dir dist
```

אמור להיות:
```
dist/
├── index.html              ← דף HTML הראשי
├── assets/
│   ├── index-[hash].js     ← כל הקוד JavaScript
│   ├── index-[hash].css    ← כל ה-CSS
│   └── ...
```

הקובץ `index-[hash].js` אמור להיות **כמה מגה-בייט**.

---

## שלב 3: העלאה דרך FTP 📤

### A. התחבר ל-FTP

השתמש ב-FTP client כמו:
- **FileZilla** (מומלץ - https://filezilla-project.org/)
- **WinSCP**
- **Cyberduck**

### פרטי התחברות:
- **Host:** edu-manage.org (או שרת אחר שבו האתר רץ)
- **Port:** 21 (או 22 אם SFTP)
- **Username:** [שם המשתמש שלך]
- **Password:** [סיסמה]

### B. נווט לתיקיית האפליקציה

בשרת, חפש את התיקייה שבה האפליקציה נמצאת. זה יכול להיות:
```
/public_html/
/var/www/html/
/htdocs/
/www/
```

או תיקייה עם השם `LMS` או `base44-app`.

**חשוב:** חפש תיקייה עם קובץ `index.html` בפנים - זו תיקיית הפרודקשן!

### C. גבה את התיקייה הקיימת (חשוב!)

לפני שמוחקים כלום:
1. **הורד** את כל התיקייה הנוכחית למחשב שלך
2. שמור אותה בשם `backup-[תאריך]`

זה למקרה שמשהו ישתבש.

### D. מחק את התוכן הישן

מחק את כל הקבצים **בתוך** תיקיית האפליקציה (לא את התיקייה עצמה):
- `index.html`
- תיקיית `assets/`
- כל קובץ אחר שם

### E. העלה את התוכן החדש

מהמחשב שלך:
```
C:\Users\User\Desktop\updateLMS\LMS\dist\
```

העלה את **כל התוכן** (לא את התיקייה `dist` עצמה, אלא מה שבפנים):
- ✅ `dist/index.html` → שרת: `/public_html/index.html`
- ✅ `dist/assets/` → שרת: `/public_html/assets/`

**שים לב:** העלה את התוכן **של** dist, לא את התיקייה dist.

---

## שלב 4: בדיקה 🧪

### A. נקה Cache בדפדפן

**חשוב מאוד!**

```
Ctrl + Shift + Delete
```

בחר:
- ✅ Cached images and files
- ✅ Cookies (אופציונלי)

לחץ "Clear data"

### B. פתח את האתר

פתח:
```
https://edu-manage.org/PublicView?type=assignment&id=6942ae578b6cc5827f6df40d
```

### C. בדוק Console

לחץ `F12` → טאב `Console`

**אם יש שגיאות אדומות:**
- צלם מסך
- שלח לי

**אם אין שגיאות:**
1. לחץ על "כניסה למטלה"
2. גלול למטה
3. **חפש:** "🎯 אבני דרך לפרויקט"

---

## שלב 5: אימות שזה עובד ✨

אמור לראות:
```
🎯 אבני דרך לפרויקט

1. שלב 1: בחירת נושא ומחקר ראשוני
   10% | תאריך יעד: [תאריך]
   [לחץ להרחבה]

2. שלב 2: תכנון ועיצוב מפורט
   20% | תאריך יעד: [תאריך]
   ...
```

---

## 🐛 פתרון בעיות נפוצות

### בעיה: "npm is not recognized"

**פתרון:** התקן Node.js מ-https://nodejs.org/

### בעיה: Build נכשל עם שגיאות

**פתרון:**
```bash
# נקה node_modules
rmdir /s /q node_modules
rmdir /s /q dist

# התקן מחדש
npm install
npm run build
```

### בעיה: האתר לא משתנה אחרי ההעלאה

**פתרונות:**
1. נקה cache בדפדפן (`Ctrl+Shift+Delete`)
2. נסה מצב Incognito (`Ctrl+Shift+N`)
3. בדוק שהקבצים אכן הועלו לשרת
4. בדוק שאתה בתיקייה הנכונה בשרת

### בעיה: שגיאות 404 על קבצי assets

**פתרון:** ודא שתיקיית `assets/` הועלתה לתיקייה הנכונה:
```
[שרת]/assets/index-[hash].js  ← צריך להיות כאן
```

---

## 📋 Checklist

- [ ] `npm install` הושלם בהצלחה
- [ ] `npm run build` יצר תיקיית `dist/`
- [ ] גיבוי של הקבצים הישנים בשרת
- [ ] מחיקת קבצים ישנים מהשרת
- [ ] העלאת כל תוכן `dist/` לשרת
- [ ] ניקוי cache בדפדפן
- [ ] בדיקה באתר - לחיצה על "כניסה למטלה"
- [ ] אבני דרך מוצגות! ✨

---

## 📞 צריך עזרה?

אם משהו לא עובד:
1. צלם מסך של השגיאות
2. צלם מסך של קבצי FTP בשרת
3. שלח לי

**בהצלחה! 🚀**
