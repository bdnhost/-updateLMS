# תוכנית פיתוח ושיפורים עתידיים (Future Roadmap)

מסמך זה מרכז את ההצעות לשיפור מבנה הנתונים (Schema) והפונקציונליות של המערכת, מדורג לפי עדיפות.

## 🔴 עדיפות גבוהה (High) - הכרחי ליישום מיידי

### 1. אוטומציה של מערכת שעות (CourseSchedule) ✨
*   **הצורך:** קריטי. כיום יצירת קורס דורשת הזנה ידנית של 20-30 מפגשים. זה לא סקיילבילי.
*   **הפתרון:** ישות המגדירה "חוקיות" (למשל: ימי שני ורביעי ב-10:00) ליצירה אוטומטית.
*   **שדות:** `recurrence_type` (daily/weekly), `days_of_week` (array), `start_time`, `end_time`, `start_date`, `end_date`.

### 2. הרחבת ניהול נוכחות (Attendance Extended) 🔧
*   **הצורך:** תפעולי/חוקי. המערכת חייבת לתמוך בתיעוד אישורי מחלה/מילואים ולא רק "נעדר".
*   **הפתרון:** הוספת שדות לישות הקיימת.
*   **שדות להוספה:** `excuse_reason`, `excuse_document_url`, `approved` (boolean), `approved_by` (user_id).

### 3. מחוונים להערכה (Rubrics) ✨
*   **ישויות:** `GradingRubric`, `RubricCriterion`, `RubricGrade`
*   **הצורך:** פדגוגי. ציון מספרי בודד (למשל "80") לא נותן פידבק מספק.
*   **הפתרון:** פירוק הציון למרכיבים (תוכן, יצירתיות, עמידה בזמנים).

---

## 🟡 עדיפות בינונית (Medium)

### 1. מנוע מבחנים (Quizzes) ✨
*   **ישויות:** `Quiz`, `QuizQuestion`, `QuizAttempt`
*   **הפתרון:** יצירת ישויות לניהול שאלות אמריקאיות/פתוחות ומעקב אחרי ביצועים וניסיונות.

### 2. מעקב קריאת הודעות (MessageDelivery) ✨
*   **הפתרון:** טבלת קישור בין הודעה לתלמיד עם סטטוס קריאה (`delivered`, `read`).

### 3. פורום קורס (Forums) ✨
*   **ישויות:** `ForumPost`, `ForumReply`
*   **הפתרון:** מערכת פורומים בסיסית לדיונים כיתתיים.

---

## 🟢 עדיפות נמוכה (Low / Nice to Have)

### 1. דרישות קדם (CoursePrerequisite) ✨
*   **הפתרון:** מניעת הרשמה לקורס מתקדם ללא סיום קורס בסיס.

### 2. ניהול משאבים (Resources) ✨
*   **הפתרון:** ניהול מלאי ציוד וכיתות פיזיות.